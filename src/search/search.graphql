type Query {
  search(searchInput: SearchInput!): [SearchResult]!
}

input SearchInput {
  string: String!
}

type SearchResult {
  id: ID!
  kind: SearchResultKind!
  title: String!
}

enum SearchResultKind {
  project
  phrase
  translation
}
